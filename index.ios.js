import React from 'react';
import { AppRegistry } from 'react-native';
import App from './src/App';

const mobilidade = props => (
  <App />
);

AppRegistry.registerComponent('mobilidade', () => mobilidade);
